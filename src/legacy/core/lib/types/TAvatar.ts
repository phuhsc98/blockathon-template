export enum EAvatarShape {
  Circle = 'circle',
  Square = 'square',
}

export enum EAvatarSize {
  Default = 'default',
  Small = 'small',
  Large = 'large',
}

export type TAvatarSize = EAvatarSize | number;
